{% extends "@SlimSkeleton/baselayout.html.twig" %}

{% block container %}
    <div class="row">
        <div class="col-xs-12">
            <h1>{{ messageTitle|trans(locale) }}</h1>
            <p>{{ messageText|trans(locale)|raw }}</p>
        </div>
    </div>
{% endblock %}
